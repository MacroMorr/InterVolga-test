Чем отличаются эти запросы:
1. SELECT * FROM a, b WHERE a.id=b.a_id;
2. SELECT * FROM a JOIN b ON a.id=b.a_id;

SELECT * FROM a, b WHERE a.id=b.a_id;
Выбери и пришли все записи (*) из таблицы (from) a, b условие фильтра (where)

SELECT * FROM a JOIN b ON a.id=b.a_id;
В этом случае Jhon объединяет две таблицы